upstream flask_app{
    server app:5000;
}

server{
    listen 80;

    location /{
    proxy_pass: http://flask_app;
    proxy_set_header: Host $host;
    proxy_set-header: X-Real-Ip $remote-addr;
    }
}
